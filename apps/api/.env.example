DATABASE_URL="postgresql://user:password@localhost:5432/waitr?schema=public"
JWT_SECRET="change-me-in-production"
# Optional: SMTP for sending OTP emails (if unset, OTP is logged only)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM=noreply@waitr.local
