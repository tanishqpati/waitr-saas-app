DATABASE_URL="postgresql://user:password@localhost:5432/waitr?schema=public"
JWT_SECRET="change-me-in-production"
# Access token expiry (default: 15m). Refresh token in httpOnly cookie (default: 14d).
# ACCESS_TOKEN_EXPIRY=15m
# REFRESH_TOKEN_EXPIRY=14d
# REFRESH_COOKIE_NAME=waitr_rt
# Redis for refresh token session store (optional; if unset, in-memory store is used).
# REDIS_URL=redis://localhost:6379
# Upstash Redis (REST) for menu cache, cart storage, and rate limiting. Create at https://console.upstash.com
# UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
# UPSTASH_REDIS_REST_TOKEN=xxx
# CORS origin for API credentials (default: http://localhost:3000). Set to your frontend URL in production.
# CORS_ORIGIN=https://your-app.vercel.app
# Resend: get API key at https://resend.com (if unset, OTP is logged only)
# RESEND_API_KEY=re_xxxxx
# From address: use onboarding@resend.dev for testing, or your verified domain
MAIL_FROM="Waitr <onboarding@resend.dev>"
# Log level: trace, debug, info, warn, error (default: debug in development, info in production)
# LOG_LEVEL=info
